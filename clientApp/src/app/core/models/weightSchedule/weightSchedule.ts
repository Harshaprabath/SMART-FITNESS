import { Injectable } from '@angular/core'

export class WeightSchedule {
    id: number;
    title: string;
    gymName: string;
    location: string;
    description: string;
    serviceCharge: number;
    isActive: boolean;
}